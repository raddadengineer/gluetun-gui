scratch.txt
.DS_Store
node_modules/
*.env
*.conf

# Persistent data (user-specific)
data/
sessions.json
**/gui-homelab-state.json

# Root-level env files that may contain credentials (data/ is already ignored)
gui-settings.env
gluetun.env
